# 代码规范

## 开发原则
- 以简单、条理清晰、易维护为第一原则。
- 可以高内聚、低耦合，但不追求企业级过度设计，不做过度封装，不为未来可能需求提前抽象。
- 代码以当前实际需求为准，保持最小实现；实现前先明确需求和假设，存在歧义时先确认，不要自行猜测。
- 优先给出建议和可选方案，未经明确要求，不擅自修改现有设计、逻辑或架构；修改代码时只处理与当前需求直接相关的内容。
- 每次修改都应有明确可验证结果，优先通过编译、运行或最小样例确认正确性。（注：暂时不需要你来跑编译运行，我会手动编译运行）

## 适用范围
- 适用于 App、BSP、Hardware、System、Middlewares 中的自定义代码。
- 若项目已有既定框架，则目录结构和模块组织按原框架走，自编代码的具体规范仍按本规范执行。
- 若项目没有既定代码框架、目录结构或命名约定，则自编代码默认按本规范组织和实现。
- CubeMX 生成代码保持原命名，不强行修改。

## 文件命名
- 文件名使用小写加下划线。
- 头文件与源文件一一对应。

## 函数命名
- 函数名统一采用 `模块名_动作名`。
- 模块名和动作名均使用大驼峰。
- 常见缩写可全大写，如 LED、OLED、UART、ADC、PWM。
- 同一模块内命名风格必须统一，不允许混写。

## 参数命名
- 参数名使用小写加下划线。
- 输出参数使用 `out_` 前缀。
- 开关参数使用 `enable`。
- 状态判断参数使用 `is_xxx`。
- 存在性判断参数使用 `has_xxx`。
- 能力判断参数使用 `can_xxx`。
- 带单位参数必须体现单位。

## 变量命名
- 全局变量使用 `g_` 前缀。
- 文件内静态变量使用 `s_` 前缀。
- 局部变量使用小写加下划线。
- 宏使用全大写加下划线。
- 结构体类型命名保持项目内统一。

## 返回值
- 不会失败的函数使用 `void`。
- 仅表示真假状态的函数使用 `bool`。
- 需要返回错误信息或执行结果的函数使用 `int`。
- `int` 约定：`0` 成功，负数失败，正数表示有效结果、处理字节数或特殊状态。
- 多状态结果优先使用 `enum`，不要滥用 `int`。

## 注释
- 注释统一使用中文。
- 注释说明功能、参数、返回值、约束条件。
- 避免解释显而易见的代码动作。
- 对外接口、协议解析、中断回调、时序敏感逻辑、复杂分支必须写注释。
- 简单转发、直接赋值、明显的静态函数不强制写注释。
- 函数头注释使用简洁的 Doxygen 风格。

函数头注释格式：
/**
 * @brief  功能简述
 * @param  param1    参数1说明
 * @param  param2    参数2说明
 * @return 返回值说明
 */

## 总体要求
- 规则服务于可读性和统一性，不追求形式化过度设计。
- 先建议，后改动；未经明确要求，不擅自调整现有结构、逻辑或架构。