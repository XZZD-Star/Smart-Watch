## 命名与注释规范

### 适用范围
- 适用于 App、BSP、Hardware、System、Middlewares 中的自定义代码。
- 若项目已有既定代码框架、目录结构或命名规范，优先遵循项目原有约定。
- 若项目没有明确框架，则按本规范执行。
- CubeMX 生成代码保持原命名，不强行修改。

### 文件命名
- 文件名使用小写加下划线。
- 头文件与源文件一一对应。

### 函数命名
- 函数名统一采用 `模块名_动作名`。
- 模块名和动作名均使用大驼峰。
- 常见缩写可全大写，如 LED、OLED、UART、ADC、PWM。
- 同一模块内命名风格必须统一，不允许混写。

### 参数命名
- 参数名使用小写加下划线。
- 输出参数使用 `out_` 前缀。
- 开关参数使用 `enable`。
- 状态判断参数使用 `is_xxx`。
- 存在性判断参数使用 `has_xxx`。
- 能力判断参数使用 `can_xxx`。
- 带单位参数必须体现单位。

### 变量命名
- 全局变量使用 `g_` 前缀。
- 文件内静态变量使用 `s_` 前缀。
- 局部变量使用小写加下划线。
- 宏使用全大写加下划线。
- 结构体类型命名保持项目内统一。

### 返回值
- 不会失败的函数使用 `void`。
- 仅表示真假状态的函数使用 `bool`。
- 需要返回错误信息或执行结果的函数使用 `int`。
- `int` 约定：`0` 成功，负数失败，正数表示有效结果或特殊状态。
- 多状态结果优先使用 `enum`，不滥用 `int`。

### 注释
- 注释统一使用中文。
- 对外接口、协议解析、中断回调、时序敏感逻辑、复杂分支必须写注释。
- 简单转发、直接赋值、明显的静态函数不强制写注释。
- 函数头注释使用 Doxygen 风格，简洁说明功能、参数、返回值和约束。

### 总体原则
- 代码以实用为先，不做过度封装。
- 规则服务于可读性和统一性，不追求形式化过度设计。